spring:
  datasource:
    url: jdbc:tc:postgresql:15.1:///postgres
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  sql:
    init:
      mode: always
# logging
logging:
  level:
    org.jooq: debug
    org.springframework.jdbc: debug